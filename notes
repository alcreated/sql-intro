sqlite3 baseball. squlite3
if you ever get kicked out type in sqlite3 then the name of the file you are working in

.schema
.mode columns
.headers on
.read SQL_FILENAME
.quit


SELECT
FROM
WHERE
AND 
ORDER BY (DESC)
LIMIT 


SELECT product, AVG(rating)
FROM reviews
WHERE
AND 
GROUP BY 
ORDER BY (DESC)
LIMIT